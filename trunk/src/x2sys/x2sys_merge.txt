#include "common_options.txt"
.TH X2SYS_MERGE 1 MANDATE GMT_STRING "Generic Mapping Tools"
.SH NAME
x2sys_merge \- Merge an updated COEs tables
.SH SYNOPSIS
\fBx2sys_merge\fP OPT(A)\f<Imain_COElist.d>\fP OPT(M)\f<Inew_COElist.d>\fP  
.SH DESCRIPTION
\fBx2sys_merge\fP will read two crossovers data base and output the contents
of the main one updated with the COEs in the second one. The second
file should only contain updated COEs relatively to the first one.
That is, it MUST NOT contain any new two tracks intersections (This point is
NOT checked in the code). This program is usefull when, for any good reason like
file editing NAV correction or whatever, one had to recompute only the COEs between
the edited files and the rest of the database.
.TP
OPT(A)\f<main_COElist.d>\fP
Specify the file with the main crossover error data base.
.TP
OPT(M)\f<new_COElist.d>\fP
Specify the file with the newly computed crossover error data base.
.SH OPTIONS
No space between the option flag and the associated arguments.
.TP
.SH EXAMPLES
To update the main COE_data.txt with the new COEs estimations saved in the smaller COE_fresh.txt, try
.br
.sp
\fBx2sys_merge\fP OPT(A)COE_data.txt OPT(M)COE_fresh.txt > COE_updated.txt
.br
.sp
.SH "SEE ALSO"
.IR x2sys_binlist (1),
.IR x2sys_cross (1),
.IR x2sys_datalist (1),
.IR x2sys_get (1),
.IR x2sys_init (1),
.IR x2sys_list (1),
.IR x2sys_put (1),
.IR x2sys_report (1)
